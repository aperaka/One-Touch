# Optional for local development. If omitted, One Touch uses local JSON storage.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DATABASE

# Set this in production so cookies are sent only over HTTPS.
NODE_ENV=production

# Hosting providers usually set PORT automatically.
PORT=3000
